{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ContentBrief",
  "type": "object",
  "required": ["topic", "slug", "angle", "target_keywords", "hooks", "story_format", "story_structure"],
  "properties": {
    "topic": { "type": "string" },
    "slug": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "angle": {
      "type": "string",
      "description": "The unique narrative angle: what insight does this give the audience they haven't seen framed this way before?"
    },
    "target_audience": { "type": "string" },
    "pain_points": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Emotional or practical frustrations this content addresses"
    },
    "target_keywords": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" }
    },
    "tone": { "type": "string" },
    "hooks": {
      "type": "array",
      "minItems": 2,
      "items": { "type": "string" },
      "description": "2-3 hook options that would stop someone scrolling Instagram — emotionally resonant, curious, relatable"
    },
    "instagram_angle": {
      "type": "string",
      "description": "The Instagram-specific framing: what emotional journey does the viewer go through?"
    },
    "story_format": {
      "type": "string",
      "enum": ["steps", "top5"],
      "description": "steps = 4 slides (intro + 3 steps); top5 = 6 slides (intro + top 5 items)"
    },
    "story_structure": {
      "type": "object",
      "description": "Outline of what each story slide communicates",
      "properties": {
        "intro": {
          "type": "string",
          "description": "What the intro slide communicates — the hook and the promise"
        },
        "slides": {
          "type": "array",
          "items": { "type": "string" },
          "description": "One-sentence description of what each subsequent slide covers (3 for steps, 5 for top5)"
        }
      },
      "required": ["intro", "slides"]
    }
  }
}
